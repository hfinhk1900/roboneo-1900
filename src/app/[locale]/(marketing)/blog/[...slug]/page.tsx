import { notFound } from 'next/navigation';

// Placeholder page to satisfy routing; real blog rendering is handled elsewhere.
// Remove or implement when blog feature is ready.
export default function BlogPostPage() {
  notFound();
}
